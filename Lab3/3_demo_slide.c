#include <stdio.h>

int main() {
    /* Xếp loại dựa theo điểm trung bình
    double dtb;
    printf("Nhap diem trung binh: ");
    scanf("%lf", &dtb);
    if (dtb >= 9) // {...} khối lệnh 2 dòng trỏ lệnh
        printf("Xuat sac\n");
    else if (dtb >=8)
        printf("Gioi\n");
    else if (dtb >=7) 
        printf("Kha\n");
    else if (dtb >=5) 
        printf("Trung binh\n");
    else
        printf("Yeu\n");*/

    /*Khoanh đáp án đúng
    printf("Viet Nam có bao nhiêu dân tộc anh em?:");
    printf("a. 52\nb. 53\nc. 54\nd. 55\n");
    printf("Xin mời bạn chọn đáp án (a,b,c,d): ");
    char dapAn;
    scanf("%s",&dapAn);
    switch(dapAn){
        case 'a': printf("Ban da chon sai\n");
        break;
        case 'b': printf("Bạn đã chọn sai\n");
        break;
        case 'c': printf("Bạn đã chọn đúng\n");
        break;
        case 'd': printf("Bạn đã chọn sai\n");
        default:
            printf("Bạn chọn không đúng 4 đáp án (a,b,c,d)");
    }
    */
    return 0;
}

