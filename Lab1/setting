// Hướng dẫn cài đặt phần mềm VS Code cho lập trình C/C++ trên hệ điều hành Windows
// Bước 1: Tải và cài đặt Visual Studio Code
// Truy cập trang web chính thức của Visual Studio Code tại https://code.visualstudio.com/
// Tải xuống trình cài đặt phù hợp với hệ điều hành Windows của bạn và tiến hành cài đặt theo hướng dẫn trên màn hình
// Bước 2: Cài đặt trình biên dịch C/C++
// Tải và cài đặt MinGW-w64 từ trang web https://www.mingw-w64.org/
// Trong quá trình cài đặt, chọn kiến trúc phù hợp (x86_64 cho hệ 64-bit hoặc i686 cho hệ 32-bit)
// Sau khi cài đặt xong, thêm đường dẫn đến thư mục 'bin' của MinGW-w64 vào biến môi trường PATH của hệ thống
// Bước 3: Cài đặt tiện ích mở rộng C/C++ cho VS Code
// Mở Visual Studio Code, truy cập vào phần Extensions (hoặc nhấn Ctrl+Shift+X)
// Tìm kiếm "C/C++" và cài đặt tiện ích mở rộng do Microsoft phát triển
// Bước 4: Cấu hình trình biên dịch trong VS Code
// Mở thư mục dự án của bạn trong VS Code
// Tạo một tệp có tên "tasks.json" trong thư mục ".vscode"
// Thêm cấu hình sau vào tệp "tasks.json" để thiết lập trình biên dịch C/C++
/*
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build C/C++",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Task generated by VS Code."
        }
    ]
}
*/
// thêm cấu hình sau vào tệp "launch.json" để thiết lập cấu hình chạy chương trình C/C++
/*
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C/C++: g++.exe build and debug active file",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "C:/mingw-w64/mingw64/bin/gdb.exe", // Cập nhật đường dẫn này theo vị trí cài đặt gdb của bạn
            "preLaunchTask": "build C/C++",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}
*/
// Bước 5: Viết và chạy chương trình C/C++
// Tạo một tệp mới với phần mở rộng .c hoặc .cpp trong VS Code
// Viết mã nguồn C/C++ vào tệp và lưu lại
// Sử dụng tổ hợp phím Ctrl+Shift+B để biên dịch chương trình
// Sử dụng F5 để chạy và gỡ lỗi chương trình
// Chúc bạn thành công trong việc cài đặt và sử dụng VS Code cho lập trình C/C++ trên Windows!